#### 主键类型用long

#### 整型数字直接用int，不需要长度

#### generated的annotation不需要，这个由人工添加

#### 不要添加任何注释

#### 数据用number表示，要指明长度和精度

#### string也要指明长度

#### 实体对象（单个主键）持久化名称由tn_<模块建成>_<每个单词的缩写>

#### 值体对象（多个主键，并且存在其他非主键属性）持久化名称由tv_<模块建成>_<每个单词的缩写>

#### 关联对象（多个主键，不存在其他非主键属性）持久化名称由tx_<模块建成>_<每个单词的缩写>

#### 常量对象（单个主键）持久化名称由tc_<模块建成>_<每个单词的缩写>

#### 属性的持久化名称同样是缩写，属性是每个单词的缩写后再组合，不需要下划线

#### 枚举类型，示例：enum[ACT:ACTIVE('启用'),DIS:DISACTIVE('禁用')]

#### 所有都要加上@name
